name: Check Links

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 8 * * 1'  # every Monday at 8am

jobs:
  check-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Render site
        run: quarto render --to html

      - name: Check links
        uses: lycheeverse/lychee-action@v2
        with:
          args: >
            --root-dir ${{ github.workspace }}/_site
            --method get
            --exclude-loopback
            --exclude 'localhost'
            --exclude 'analytics.cal-adapt.org'
            --exclude '^file://.*\.pdf$'
            --accept 200..=299,401,403,405,999
            --insecure
            --timeout 30
            --retry-wait-time 5
            --max-retries 3
            '_site/**/*.html'
          fail: true
